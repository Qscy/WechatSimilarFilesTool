namespace WinFormsApp3
{
    public partial class InitWindow : Form
    {
        public InitWindow()
        {
            InitializeComponent();
        }

        private void InitWindow_Load(object sender, EventArgs e)
        {
            var WecahtPath = $"{Environment.GetFolderPath(Environment.SpecialFolder.MyDocuments)}\\WeChat Files\\";
            var subPath = "\\FileStorage\\File\\";
            List<string> userList = new();
            List<string> monthList = new();
            List<string> fileList = new();
            var subFolders = Path.s;
            //foreach (string folder in )
            {

            }
        }
    }
}